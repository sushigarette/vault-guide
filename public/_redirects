# Redirection pour les routes React
# Toutes les routes qui ne correspondent pas à un fichier statique
# doivent rediriger vers index.html pour que le routing React fonctionne

# Pas besoin d'entrées spécifiques, la configuration Nginx gère ça
# avec try_files $uri $uri/ /index.html;
